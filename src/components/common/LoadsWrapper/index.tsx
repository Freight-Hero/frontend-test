import { FC } from 'react';
import { LoadsWrapperProps } from './types';



export const LoadsWrapper: FC<LoadsWrapperProps> = () => {
  return (
    <div className='bg-white rounded-sm overflow-hidden p-4 w-full'>
      <p>Loads Wrapper</p>
    </div>
  );
};